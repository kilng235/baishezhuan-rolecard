## 服饰描述动态化改造计划

### 背景
用户提供参考范式：「贴身小袄退至腰间，半遮半掩。藕荷色罗裙推在床脚。三寸金莲赤裸，绣花鞋散落在床下。未穿亵衣亵裤。」要求服饰描述全部动态化（衣物+位置+状态三要素），替代当前静态清单式描写。已确认：全部场景动态化，initvar 初始值保持整齐。

### 改动文件（3 个）

**1. `cards/白蛇传/世界书/扮演准则/服饰指导.yaml`**
- 新增「动态化描写」小节：
  - 三要素：每件衣物 = 衣物名 + 位置/去向 + 状态（半遮半掩程度）
  - 日常动态：行走裙裾轻晃、风吹披帛扬起、当班挽袖——拒绝"白绫百褶裙(裙摆绣白兰)"式静止清单
  - 褪衣动态：按参考范式写"退至腰间/推在床脚/散落床下/赤裸/未穿"，承接 NSFW 剥离层次（褙子→外衫→中衣→肚兜亵裤）
- 保留：分层体系（知识底座）、更衣逻辑（不随场景凭空换装）、NSFW 剥离层次

**2. `cards/白蛇传/世界书/变量/变量更新规则.yaml`**
- 白素贞/小青两处服饰 check 追加：动态化描写（衣物+位置+状态），不用纯清单；褪衣场景逐层承接剥离层次

**3. `cards/白蛇传/创作规划.yaml`**
- 服饰指导 purpose 与 variables 段两个服饰 description 同步"动态化描写"表述

### 不改动
- `initvar.yaml` / `schema.ts` 初始值（断桥初见保持整齐定妆，作为剥离起点）
- 分层体系、更衣逻辑、剥离层次

### 验证
- `validate-mvu` 校验通过
- `pack` 重新打包，确认产物含 19 条世界书