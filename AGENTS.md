# 项目规则（桃花 · 角色卡创作）

## 项目说明
本项目用于创作角色卡（SillyTavern 角色卡 / 世界书 / 叙事内容等）。
当前项目含两张角色卡：
- **白蛇传**（南宋临安背景，白素贞与小青双角色，MVU Zod 变量驱动）
- **梅卿**（南宋与现代双时空切片，千年梅树花妖·梅卿单角色，转世恋人纯爱线，MVU Zod 变量驱动）

## 铁律：文件产出范围
- **所有文件产出只能写入本项目目录内**（`E:\笔记\写卡\桃花`）。
- 禁止在本项目目录之外创建、修改、删除任何文件。
- 禁止写入系统目录、用户主目录、其他项目目录、临时目录等任何项目外位置。
- 需要临时文件时，一律放在本项目目录内的临时子目录中，用完自行清理。

## 通用约定
- 文字内容默认使用中文书写。
- 新建文件时，遵循项目的目录结构约定；无明确约定时按内容合理组织。
- 重要产出（角色卡 JSON、PNG 卡、世界书等）保留在项目内便于追溯版本。

## 知识库（外部参考，只读）
创作时可查阅以下知识库，均位于项目目录之外，**仅作参考、禁止写入**：
- `E:\笔记\写卡\STDB` — ST 开发指南数据库（通用 SillyTavern 开发参考，A0–E5 系列：A2 角色卡格式规范 / A3 世界书优化 / A5 渲染管线与宏 / A6 正则机制 / B1 变量更新规则 / C 系列状态栏与前端 / D 系列性能部署与安全 / E 系列扩展生态）
- `E:\笔记\写卡\写卡经验` — 角色卡拆解与经验总结（card_disassembly / APP_12_disassembly / 状态栏脚本开发报告等）
- `E:\笔记\写卡\参考` — 参考资源库（manifest.json 组件目录：character-info / extensions / helper-scripts / regex-scripts / worldbook / sandbox）

## 项目结构

```
cards/
├── 白蛇传/
│   ├── 白蛇传.json              # 最终角色卡 JSON（chara_card_v3）
│   ├── schema.ts                # MVU Zod 变量 schema（编译后嵌入 JSON）
│   ├── tavern-cards-state.json  # 卡片状态管理文件
│   ├── 创作规划.yaml             # 创作大纲
│   ├── 开场白/0.txt              # 开场白（first_mes）
│   ├── 正则/                    # 状态栏/变量更新的正则替换 HTML
│   │   ├── 状态栏界面.html
│   │   ├── 变量更新中美化.html
│   │   └── 变量更新美化.html
│   ├── 脚本/MVU.txt             # MVU 脚本源文件
│   ├── 世界书/                  # 世界书条目（YAML 源文件）
│   │   ├── 变量/               # MVU 变量相关（initvar/更新规则/输出格式/变量列表）
│   │   ├── 扮演准则/            # 扮演准则、服饰指导
│   │   ├── 角色/               # 白素贞、小青、许仙的角色信息
│   │   ├── 世界观/              # 世界设定、修行体系扩展（狐修/鬼修/道术）
│   │   ├── 地理/               # 临安城、茅山派、临安城隍庙、勾栏瓦舍、荒宅废园、城隍庙
│   │   ├── 时间线/              # 故事前史
│   │   └── NPC/                # 许娇容、李公甫、法海、苏媚娘（狐妖）、秋棠（女鬼）
│   └── .patch-history/          # 自动保存的 JSON 修改历史
│
└── 梅卿/
    ├── 梅卿.json                # 最终角色卡 JSON（chara_card_v3）
    ├── schema.ts                # MVU Zod 变量 schema（编译后嵌入 JSON）
    ├── 创作规划.yaml             # 创作大纲
    ├── 开场白/0.txt              # 开场白（first_mes）
    ├── 正则/                    # 状态栏/变量更新的正则替换 HTML
    │   ├── 状态栏界面.html
    │   ├── 变量更新中美化.html
    │   └── 变量更新美化.html
    ├── 脚本/MVU.txt             # MVU 脚本源文件
    ├── 世界书/                  # 世界书条目（YAML 源文件）
    │   ├── 变量/               # MVU 变量相关（initvar/更新规则/输出格式/变量列表）
    │   ├── 扮演准则/            # 扮演准则、服饰指导
    │   ├── 角色/               # 梅卿角色信息、前世恋人设定
    │   ├── 世界观/              # 世界设定（草木成精/转世轮回/花妖四季）
    │   ├── 地理/               # 梅园（古代）、现代都市
    │   ├── 时间线/              # 梅卿千年修行与前世情缘前史
    │   └── NPC/                # 无独立 NPC（转世恋人为 <user> 扮演）
    └── .patch-history/          # 自动保存的 JSON 修改历史
```

## 角色卡技术栈

- **格式**: SillyTavern chara_card_v3（spec 3.0）
- **变量系统**: MVU（MagVarUpdate） + Zod schema
- **状态栏**: 嵌入式 HTML/CSS/JS，事件驱动渲染，通过正则替换注入
- **变量更新协议**: JSON Patch (RFC 6902)，通过 `<UpdateVariable>` 标签
- **世界书**: 内嵌于角色卡（character_book），29 条目（含新增的聊斋志异元素：狐修/鬼修/道术体系、茅山派/城隍庙势力、勾栏瓦舍/荒宅废园/城隍庙场景、狐妖苏媚娘/女鬼秋棠 NPC）

## 变量系统（MVU Zod）关键约定

### 白蛇传
- 顶层变量键：`stat_data`，含 `系统变量`、`白素贞`、`小青` 三个对象
- 服饰字段拆分为 7 个子键：`外衣`、`内搭`、`贴身层`、`下装`、`鞋袜`、`发饰`、`配饰`
- 贴身程度顺序（由内到外）：贴身层 > 内搭 > 下装 > 外衣 > 鞋袜 > 发饰 > 配饰
- 好感度范围：0-100，低(0-29 青碧) / 中(30-69 金黄) / 高(70-100 朱红)
- 初始好感度：白素贞 60，小青 20
- 更新规则文件：`世界书/变量/变量更新规则.yaml`（每个子键有独立 check）
- 输出格式：`世界书/变量/变量输出格式.txt`（JSON Patch 模板）

### 梅卿
- 顶层变量键：`stat_data`，含 `系统变量`、`梅卿` 两个对象
- 时空切片：`系统变量.时空切片`（古代/现代，玩家选择开局）
- 服饰字段同样拆分为 7 个子键（与白蛇传一致）
- 好感度范围：0-100，颜色规则与白蛇传相同
- 初始好感度：梅卿 65（前世情缘加持，初见即有基础好感）
- 梅卿专属配色：主色月白/素白/雪青，点缀梅红/霜色

## 状态栏渲染

### 白蛇传
- 文件：`正则/状态栏界面.html`
- 通过 `render()` 函数从 `stat_data` 实时读取并填充
- 服饰使用 `renderClothes(d, prefix, containerId)` 分行渲染
- 好感度使用 `dots(containerId, numId, val)` 渲染 10 格圆点
- 订阅 `Mvu.events.VARIABLE_UPDATE_ENDED` 事件驱动刷新

### 梅卿
- 文件：`正则/状态栏界面.html`
- 通过 `render()` 函数从 `stat_data` 实时读取并填充
- 单角色渲染（梅卿），根据 `时空切片` 变量动态切换古代/现代两套界面（头部标题 + 数据字段）
- 头部显示当前时空切片（古代/现代）
- 服饰使用 `renderClothes(d, prefix, containerId)` 分行渲染
- 好感度使用 `dots(containerId, numId, val)` 渲染 10 格圆点
- 订阅 `Mvu.events.VARIABLE_UPDATE_ENDED` 事件驱动刷新

## 开场白

### 白蛇传
- 源文件：`开场白/0.txt`
- 角色名使用 `{{user}}` 占位（而非"许仙"）
- 需同步更新 `白蛇传.json` 中的 `first_mes`（顶层 + data 层）

### 梅卿
- 源文件：`开场白/0.txt`（古代）、`开场白/1.txt`（现代，alternate_greetings）
- 角色名使用 `{{user}}` 占位（转世恋人，古代切片为梅园旧主/文人等）
- 现代切片开场白配 `开场白/initvar/1.yaml`（initvar_override，时空切片=现代）
- 需同步更新 `梅卿.json` 中的 `first_mes`（顶层 + data 层）和 `alternate_greetings`

## 打包发布

### 白蛇传
- 最终产出：`白蛇传.json`（手工编辑或程序化更新）
- 无 PNG 打包流程（无外壳图片资源）
- 正则脚本共 5 个：对AI隐藏状态栏、状态栏界面、对AI隐藏变量更新、变量更新中美化、变量更新美化
- 助手脚本共 2 个：MVU（变量引擎）、Zod（变量 schema）
- 修改任一源文件（initvar/schema/HTML/更新规则）后必须同步更新 JSON 卡中嵌入的对应内容

### 梅卿
- 最终产出：`梅卿.json`（手工编辑或程序化更新）
- 无 PNG 打包流程（无外壳图片资源）
- 正则脚本共 5 个：对AI隐藏状态栏、状态栏界面、对AI隐藏变量更新、变量更新中美化、变量更新美化
- 助手脚本共 2 个：MVU（变量引擎）、Zod（变量 schema）
- 修改任一源文件（initvar/schema/HTML/更新规则）后必须同步更新 JSON 卡中嵌入的对应内容

## 已知注意事项

- JSON 卡中 `tavern_helper.scripts[1]`（Zod）和 `regex_scripts[1]`（状态栏界面）嵌入了编译后的代码，修改源文件后需同步更新
- 服饰指导规范（`世界书/扮演准则/服饰指导.yaml`）的分层体系必须与 initvar 和 schema 保持一致
- 开场白修改后需同时更新对应 JSON 卡的顶层 `first_mes` 和 `data.first_mes`
- 变量更新规则中每个子键的 check 描述需与 initvar 实际内容对齐，避免残留旧词
- **白蛇传** 和 **梅卿** 是两张独立角色卡，各自有独立的 schema、initvar、正则、世界书，修改时注意区分目录
- 两张卡共享同一个 MVU 引擎（`脚本/MVU.txt` 引用同一 CDN），但 Zod schema 和状态栏界面各自独立编译嵌入
